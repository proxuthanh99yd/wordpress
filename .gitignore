# Sinh tự động bởi wizard (setup.sh) — per-deploy, KHÔNG commit
# (env.example là bản mẫu tham chiếu, vẫn được track)
.env
docker-compose.yml

# MySQL data directory
mysql-data/

# Redis data directory
redis-data/

# WordPress uploads and cache
public_html/wp-content/uploads/
public_html/wp-content/cache/

# Nginx config sinh bởi setup.sh (per-deploy)
*.conf

# Backups (sinh bởi backup.sh)
backups/

# Logs
*.log

# OS files
.DS_Store
Thumbs.db

